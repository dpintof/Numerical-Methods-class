function y = func(x)
% x is the function's input
% y is the function's output

% The idea is to define a function in this file. Then we use that function
% in exercices such as 9 and 10.

y = exp(x) - 2;

end

% Testar, para y = exp(x):
% func(3)

% Resultado:
% 20.0855

% Verificar resultado
% exp(3)